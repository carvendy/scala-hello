package com.carvendy.hello.scala

object TestCheckSum {
 
  def main(args: Array[String]): Unit = {
    val one = new CheckSum();
    val two = new CheckSum();
   // one.sum = 1
   // two.sum = 2
  }
}